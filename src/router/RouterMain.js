/* eslint-disable no-useless-constructor */import React, {Component} from "react"import {connect} from "react-redux"import {Router, Route, Switch, Redirect} from "react-router-dom"import thunk from "../store/thunk"import {ToggleSidebar, GenerateRoutes, RequireUserInfo} from "../store/config/actionTypes"class RouterMain extends Component {    constructor (props) {    	super(props)    }    componentDidMount(){	    const {dispatch} = this.props	    dispatch(thunk(RequireUserInfo))    }    render() {        return (	        <Switch>		        {		        	this.props.primaryRoutes.map(route => route.redirect ?			        <Redirect key={route.path} from={route.path} to={route.redirect} exact /> :			        <Route key={route.path} path={route.path} component={route.component} exact={!route.noExact} redirect={route.redirect} />)		        }	        </Switch>        )    }}let routerMainConnect = connect(	(state) => ({		primaryRoutes: state.permission.primaryRoutes,		roles: state.user.roles,		$state: state	}))export default routerMainConnect(RouterMain)