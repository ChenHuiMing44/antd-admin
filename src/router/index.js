/* eslint-disable no-useless-constructor */import React, {Component} from "react"import {Router, Route, BrowserRouter, Switch, Redirect} from "react-router-dom"import renderRoutes from "./renderRoutes"import CusLayout from "./../layout/CusLayout"import Login from "./../views/login/Login"//let auth = true;class CusRouter extends Component {    constructor (props) {    	super(props)    }    render() {        return (            <BrowserRouter>	            <Switch>		            <Route path={"/login"} component={Login} />		            <Route path={"/"} render={() => auth ? <CusLayout />: <Redirect to={"/login"} />} />	            </Switch>            </BrowserRouter>        )    }}export default CusRouter