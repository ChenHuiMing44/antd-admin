/* eslint-disable no-useless-constructor */import React, {Component} from "react"import {	Form, Button} from 'antd';import "./login.css"import {connect} from "react-redux"import thunk from "./../../store/thunk"import * as ActionTypes from "./../../store/config/actionTypes"// console.log(connect);class Login extends Component {    constructor (props) {    	super(props);	    this.state = {		    userName: "",		    password: "",		    openPassword: false	    }    	this.inputUserName = this.inputUserName.bind(this);    	this.inputPassword = this.inputPassword.bind(this);    	this.toggleShowPassword = this.toggleShowPassword.bind(this);    	this.login = this.login.bind(this)    }	inputPassword(e){		this.setState({			password: e.target.password		})	}    inputUserName(e){	    this.setState({		    userName: e.target.value	    })    }    toggleShowPassword(){    	let open = this.state.openPassword;    	this.setState({		    openPassword: !open	    })    }	login(){    	//点击登陆		const {dispatch, history} = this.props;	    setTimeout(() => {			// debugger	    	dispatch(thunk(ActionTypes.LogBackByToken, this.state.userName));			// this.$router.push("/");		    history.push("/");	    },1000)	}    render() {    	const {theme, appName} = this.props.$state.app;    	console.log(appName);        return (	        <div className="log-container" style={{backgroundColor: theme.loginTheme}}>		        <div className={"form-box"}>			        <Form>				        <p className={"title"}>{appName}</p>				        <div className={"field"}>					        <i className="left-icon iconfont icon-yonghu"/>					        <div className="input-box">						        <input placeholder="输入用户名" defaultValue={this.state.userName} onInput={this.inputUserName}/>					        </div>				        </div>				        <div className="field">					        <i className="left-icon iconfont icon-iconfontlock"/>					        <div className="input-box">						        <input placeholder="输入密码" defaultValue={this.inputPassword} type={this.state.openPassword ? 'text' : 'password'}/>					        </div>					        <i className={'right-icon iconfont '  + (this.state.openPassword? 'icon-yanjing': 'icon-yanjing1')} onClick={this.toggleShowPassword} />		                </div>				        <Button className="login-btn" type="primary" onClick={this.login}>登陆</Button>			        </Form>		        </div>            </div>        )    }}let loginConnect = connect(	(state) => ({		$state: state	}))// debuggerexport default loginConnect(Login)