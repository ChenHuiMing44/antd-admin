import {theme, appName} from "../../config/config"import reduces from "./../reduces"import * as ActionTypes from "./../config/actionTypes"export default {	state: {		sidebarOpen: true,		theme: theme,		appName,	},	mutations: {		TOGGLE_SIDEBAR: state => {			state.sidebarOpen = !state.sidebarOpen;			// debugger			return state;		},		CLOSE_SIDEBAR: state => {			state.sidebarOpen = false;			return state		},		OPEN_SIDEBAR: state => {			state.sidebarOpen = false;			return state		}	},	actions: {		[ActionTypes.ToggleSidebar]: function ({dispatch}) {			dispatch({				type: "TOGGLE_SIDEBAR"			})		}	}}